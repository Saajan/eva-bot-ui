import React from "react";
import Layout from "layouts";

const DynamicDashboard = () => {
    return (
        <React.Fragment>
            <h1>Dashboard</h1>
        </React.Fragment>
    );
};

DynamicDashboard.layout = Layout;

export default DynamicDashboard;
